# NIHub Attendance App

Mobile app workspace for the NIHub attendance system.

## Development

```bash
npm install
npm run start
```

## Commands

- `npm run android` to open Android
- `npm run ios` to open iOS
- `npm run web` to run the web build
- `npm run lint` to check code quality
